from v1.templates import discrete_conway, Rule110, colors, gray, color_conway, chicken_test, test_high, test_dim, test_pygame, test_pygame_nd, test_pygdim

from v1.wings import pygame_engine

pyg = pygame_engine

if __name__ == "__main__":
    # chicken_test(pyg)
    # Rule110(pyg)
    gray(pyg)
    # colors(pyg)
    # discrete_conway(pyg)
    # color_conway(pyg)
    # test_high(pyg)
    # test_dim(pyg)
    # test_pygame(pyg)
    # test_pygame_nd(pyg)
    # test_pygdim(pyg)

    # from v1.raptor import Raptor
    # rule = [0,1,0,1,1,1,1,0]
    # a = Raptor(rule, 2)
    # print(a.neighbor_count)
    # print(a.io([1,1,1]))
    # print(a.io([1,1,1]))
